name: backtest confluence weight sweep (data/raw H4, sin MT5) — Ítem C
module: backtest
fixture: backtest_confluence_sweep_fixture.yaml
expected:
  module: backtest
  status: ok
  # NOTA: el adapter 'backtest' actual IGNORA confluence_weights (no cableado).
  # Este escenario es ejecutable SOLO tras aplicar el Ítem C a signals/pipeline.py.
  # La aserción 'ok' verifica que el barrido corre; el análisis de PF/WR por
  # combinación vive en el reporte JSON del adapter, no en 'expected'.
tags:
  - backtest
  - item-c
  - confluence-weights
  - raw-dataset
  - no-mt5
  - sweep
