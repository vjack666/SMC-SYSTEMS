# CRONOGRAMA Y ROADMAP - SMC-SYSTEMS

**Proyecto:** SMC-SYSTEMS (renombrado desde SMC_SUCCESSOR)
**Repositorio:** https://github.com/vjack666/SMC-SYSTEMS
**Versión del Roadmap:** 2.12 (Dashboard performance optimization: data_cache + M5 skip + canonical no reload + maps on-demand + crono tail + thread pool persistent)
**Fecha de Actualización:** 2026-07-28 (Dashboard: 7 cuellos de botella identificados y corregidos — maps on-demand vía botón "🔄 Regenerar", M5 ya no se carga 2 veces, _canonical_plan usa cache, ThreadPoolExecutor persistente, crono_widget tail reader, gc.collect() periódico, nuevo módulo data_cache.py)
**Estado General:** 🟢 Observador FundedNext operativo. **R4 CERRADO (2026-07-17): ICT puro mecánico SIN edge para live/fondeo** — ver `docs/auditorias/R4_CIERRE_FUNDING_2026-07-17.md`. **R7 UNIFICACIÓN COMPLETADA (2026-07-19):** motores legacy BOS/CHOCH/TREND eliminados (`git rm detectors/bos.py/choch.py/trend.py`); única fuente de verdad `ict_backtest/market_structure.py`. Migración validada por `scripts/diag_etapas.py` (la secuencia H4→M15 vive: run_sequence genera señales). Diseño de temporalidades y roadmap por capacidades en `docs/plan/ARQUITECTURA_TEMPORALIDADES.md` + `docs/plan/ROADMAP_CAPACIDADES.md`. **R10 cerrado (2026-07-21): default canónico `bos_gap=None`/dinámico, sin ATR/indicadores. R3.5 cerrado (2026-07-21): POI tiers/stacking documentados + `ict_backtest/tier_engine.py`. Dealing range/EQ cerrado (2026-07-21): API explícita en `ict_backtest/dealing_range_motor.py`, EQ ambiguo no descarta.**

> **NOTA DE AUDITORÍA (2026-07-21):** este plan está auditado por archivo y sección en
> `docs/plan/AUDITORIA_DOCUMENTAL_2026-07-21.md` (estado ✅ completo / ❌ incompleto /
> ⚠️ faltante).
>
> **NOTA DE RECONCILIACIÓN (2026-07-25, `AUDITORIA_COMPARATIVA_2026-07-25.md`):** las filas
> de setups marcadas 🟢 CERRADO significan *módulo cableado / anota flag* (PASO POST, knob
> apagado), NO *regla de tesis EJECUTADA en el edge*. La `AUDITORIA_FIDELIDAD_TESIS_SMC_SYSTEMS.md`
> marca entry=close BOS (bug#1, tesis §11 ❌) y TP cluster HTF (bug#2, tesis §13 ❌) como
> divergencias altas. **bug#1 y bug#2 están siendo corregidos por OTRO agente en código**; este
> doc solo registra la deuda documental. Hitos genuinamente cerrados (R1/R2/R3.5/R7/Fase1/SL
> estructural/G1+G2+G3) NO se tocan. Ver notas de deuda en filas C2/C3/D1/E1/B3/RR/KZ-2.

> **NOTA DE MANTENIMIENTO (2026-07-28, Dashboard performance: 7 cuellos de botella corregidos):** auditoría de render/update de los 13 widgets del dashboard identificó 7 causas de lag al cambiar pestañas. Fixes: (1) mapas matplotlib eliminados del ciclo automático → botón "🔄 Regenerar" on-demand en mapa_widget.py; (2) M5 se cargaba y analizaba 2 veces por ciclo (PASS 2 + loop scalping) → skip en scalping loop si ya cargado; (3) `_canonical_plan` recargaba parquets de disco → usa `data_cache` primero; (4) ThreadPoolExecutor creado/destruido por ciclo con `shutdown(wait=False)` → executor persistente global; (5) `crono_widget._ciclos_hoy()` leía `read_text` completo de `app.log` → `_tail_lines()` desde el final del archivo; (6) sin `gc.collect()` → agregado al final de cada `run_cycle`; (7) sin cache de DataFrames entre ciclos → nuevo módulo `app_observador/core/data_cache.py` con store/get/clear por (symbol, tf) y verificación de mtime. Commiteado sin push (pendiente de revisión Ruben).
>
> **NOTA DE MANTENIMIENTO (2026-07-27, feature semi-automatización DEMO + semáforo):** nueva pestaña 3 "Auto" en el observador (botón maestro ON/OFF, default OFF) que enciende/apaga `scripts/run_demo_grid.py` vía `app_observador/core/process_control.py`. El launcher corre el `PaperTradingRunner` existente en modo PAPER (DEMO, sin órdenes reales), EURUSD M15, con grid Bollinger de 3 capas (`strategy/live_grid.py`: L1=0.30/L2=0.20, step=10p, period=20/std=2.0) y tope por operación en `risk/governor.py`: ganancia fija +$60 (PROFIT_LIMIT) y pérdida 2% del saldo (LOSS_LIMIT = 0.02 × balance). El runner hace EXIT limpio al cumplir "la meta" (PROFIT_LIMIT/LOSS_LIMIT) vía flag `_goal_reached`.
> **Semáforo de seguimiento (mismo commit):** mientras el bot está ENCENDIDO, la pestaña Auto muestra en vivo las 4 fases del estocástico M15 que evalúa el bot para entrar — cada fase se enciende verde al cumplirse y queda roja si no: (1) PRECIO EN EXTREMO, (2) GIRO LISTO, (3) SEÑAL FIRME, (4) TENDENCIA A FAVOR. Cuando las 4 están en verde, el banner dice "¡MOMENTO DE ENTRAR!". El semáforo se calcula en la propia pestaña leyendo MT5 en vivo (mismo datos y lógica que el runner), en paralelo y sin acoplarlo. Verificado: 20 tests (autopilot + semaphore + live_grid + paper_sim) + render offscreen revisado. **Trading real (LIVE) sigue OFF hasta autorización explícita** (Principio 7).
>
> **NOTA DE MANTENIMIENTO (2026-07-28, feature FASE GRID en pestaña "Señal"):** la pestaña 2 "Señal" ahora conmuta sola entre dos modos según si hay operación abierta en MT5 (EURUSD). Sin posición → "FASE ESTOCÁSTICO" (el SETUP del día original). Con posición abierta → "FASE GRID ACTIVA — ESTOCÁSTICO EN PAUSA": un panel tipo semáforo monitorea en vivo las reglas del grid dictadas por el usuario (reglas REALES extraídas del binario GRID SCALP + lo dictado en chat, NO inventadas): op base del estocástico 0.50 lotes; CAPA 1 = 0.15 lotes tras 20 pips EN CONTRA (precio se aleja en pérdida: compra→baja 20p, venta→sube 20p) + toque de banda Bollinger M15 (period 20, std 2.0) en la dirección correcta (compra→banda inferior, venta→banda superior); CAPA 2 = 0.20 lotes tras OTROS 20 pips de distancia + toque de banda. El estocástico queda EN PAUSA y no busca entrada hasta que la operación cierre (profit o pérdida). El nº de capas ejecutadas se lee de `positions_get` reales (1=base, 2=capa1, 3=capa2). Verificado: 5 tests `test_senal_grid.py` (constantes, switch de fase, R1+R2 cumplidas a +25p, no-listo a -5p, capas desde posiciones) + py_compile + render offscreen revisado por visión (5 luces legibles, sin overlap). Commit + push en esta fecha.
> **NOTA DE MANTENIMIENTO (2026-07-21, commit `e227b97`):** fix de formato parquet
> (21 archivos con `time` como DatetimeIndex en vez de columna → `KeyError` en
> `sequence.py`). Scripts de descarga MT5 corregidos (`index=False`). Recovery del
> test suite: 606 passed / 10 failed (desde 576/30). Los 10 failures restantes son
> pre-existentes (pasan individualmente): 6 × suite-order pollution en A1 topdown,
> 3 × multitf_context, 1 × sklearn 1.9 vs pickle 1.7.1.
>
> **NOTA DE EJECUCIÓN (2026-07-17):** los hitos R0-R7/A1-A12 de este cronograma
> siguen vigentes como CRONOLOGÍA del proyecto. Pero la **estrategia de implementación
> orientada a la tesis** ahora vive en `docs/plan/ROADMAP_TESIS_DRIVEN_2026-07-17.md`
> (roadmap maestro). Ese doc añade Fase 0 (formalización/SPEC), Fases B-E (cierre de
> deuda conceptual), y suspende backtests de rendimiento hasta Fase G. Este
> cronograma NO se contradice; se complementa. Al commitear el roadmap maestro, este
> doc queda al día con la nota.

---

## 1. Principios Rectores (NO NEGOCIABLES)

1. **Este Cronograma es la ÚNICA fuente de verdad.** `docs/HOJA_DE_RUTA_SMC-SYSTEMS.md` quedó OBSOLETO y redirige aquí. Cualquier decisión se alinea en este documento.
2. **Harness-First Development.** Todo nuevo módulo/feature/refactor pasa por `harness/` y 100% de escenarios antes de completo.
3. **Limpieza y Enfoque.** Solo lo esencial versionado.
4. **Documentación Dual.** Consumible por humanos y agentes IA.
5. **Cierre de Ciclo con Informe Semanal.**
6. **Medir antes de afirmar.** Ningún PF sin costos ni sin re-auditar look-ahead (lección R4: 97% de velas M15/M5 contaminadas por HTF futuro en v2.7).
7. **Trader manda.** No bot de órdenes hasta A12 + autorización.

---

## 2. Estado Actual del Repositorio (2026-07-14)

### Modo de operación real
- **OBSERVADOR FUNDEDNEXT (SIN BOT):** loop `scripts/loop_analisis.py` 24/7 (lun-vie). NUNCA abre órdenes. `vigilante_riesgo.py` SOLO CIERRA (2%/4%).
- Arranque automático vía `start_hermes_session.ps1` (✅ A11 operativo).
- Código de bot heredado (`run_paper_trading.py`, `run_live_trading.py`, MQL5 EA) implementado pero NO cableado al flujo diario.

### Hallazgo crítico post-auditoría R4 (2026-07-13 → 2026-07-14)
- **Look-ahead cross-timeframe (CRÍTICO, ya corregido):** el join H4→M5 usaba velas que aún NO cerraban. Medido: **97.4% de las velas M15/M5 estaban contaminadas por precio futuro del HTF**. Los backtests "buenos" de R4 (PF 1.14 de Turtle) eran FALSO positivo — el modelo veía el futuro. Corregido en `6d4b158`/`07afc0e` (exigir barra cerrada vía `TF_FREQ` + `row_at_time`).
- Tras limpiar look-ahead, modelos R4 re-medidos (v2.7):
  - Silver Bullet: PF 0.896 / 0.639 → **RECHAZADO** (el modelo de verdad pierde).
  - PO3 + displacement: 2 y 0 trades → INCONCLUSO.
- **Turtle Soup v2.8 ALINEADO A TESIS 18 (2026-07-14):** `run_backtest.py` camino sequence usa SL mecha sweep + RR 1:3 + killzone. EURUSD M15 H4→M15: **0 señales** (1787 sweep → 170 displace → 92 BOS → 0 entry). El retorno al cuadro (mitigation) no ocurre tras el BOS con el SL estructural; el modelo no llega a operar. Veredicto: **no concluyente** (no PF<1.10, sino 0 trades). Requiere diagnóstico de por qué el `_touches_zone` no se cumple (cuadro fallback o killzone). GBPUSD pendiente.

### SL Estructural (v29, commit `e2a9c11` 2026-07-13)
- El SL ahora se ancla a la **mecha del sweep** (no ATR de fallback). Filtro `STRUCT_SL_MAX_ATR`.
- Backtest v29: EURUSD PF 1.128, GBPUSD PF 2.101 — PERO sostenidos en `hold_limit` (7/11 y 11/13 trades cerraron por hold, no TP). Rentable vs ATR v28 (<1), pero el éxito vive del hold, no del TP real.

### Tesis de ejecución óptima (2026-07-14, commit `46b074e`)
- **Libro 18** fija la regla dura: 3 capas HTF/ITF/exec; **SL y entry SIEMPRE en exec TF**; RR mínimo 1:3; 3 killzones (London/NY AM/NY PM); M5 estándar / M1 avanzado.
- Libros 15/16/17/20 corregidos a esa regla (ITF agregado, RR 1:3, M3, killzones completas).
- **Hueco de código (v30) PARCIALMENTE cerrado (2026-07-14):** el camino `sequence`/Turtle Soup en `run_backtest.py` YA usa SL estructural de mecha de sweep (`calc_structural_sl`), RR 1:3 y filtro killzone (alineado a libro 18). Falta: `build_signals_from_frames` recibir `exec_tf`/`itf` separados de `ltf` (hoy `exec_tf == ltf`) para el camino checklist/scalping y agregar M3 en `TF_FREQ`.

### Fragmentación confirmada por grafo (2026-07-14, graph.json @ 46b074e)
- 5 módulos ICT en **6 comunidades distintas** (pipeline=1/2/5/73, ict_agent=39/62, sequence=36/70, rules=57, engine=18).
- **Solo 2 aristas cruzadas** en todo el sistema: `engine.py ↔ rules.py` (el motor llama a los checklists). `pipeline.py`, `ict_agent.py`, `sequence.py` son **islas totales** (0 aristas cruzadas).
- Consecuencia: backtest (`sequence.py`/`engine.py`) y señales en vivo (`pipeline.py`) salen de motores distintos con pesos que divergen. Deuda de arquitectura (ver R7).

### Migración event-driven (borrar concepto "aged") — COMPLETA (2026-07-15)
- **Fase 0 (baseline con aged):** EURUSD medido = 28 trades, PF 1.424. GBPUSD NO medible (OOM del host en load_frames — límite de RAM transitoria, no de código).
- **Fase A (MarketObject):** `ict_backtest/market_object.py` creado. Exige `origin_tf`; prohíbe POI en LTF por construcción. TEST OK.
- **Fase B (translation.py):** capa de convivencia DataFrame↔objetos. TEST OK.
- **Fase C (data_feed.build_objects):** envuelve build_features. TEST OK.
- **Fase D (borrar aged):** CONFIRMADO MUERTO en código. En `market_structure.py` la rama de caducidad por tiempo fue eliminada; los detectores (bos/choch/ob) solo tienen comentarios de "Fase D". Tests test_no_aged fallaban antes y ahora pasan.
- **Fase E (POI HTF en sequence.py):** IMPLEMENTADO + testeado por unidad. PERO desactivado por defecto (`htf_poi_fn=None` en todo el sistema vivo). Solo lo activan el test y el script de auditoría.
- **Fase F (backtest A vs A' vs A''):** CORRIDO con datos reales.
    - A  (con aged, baseline):        28 trades | PF 1.424 | +5.7R
    - A' (sin aged, real):            37 trades | PF 1.511 | +8.9R  ← el validado
    - A'' (POI HTF como FILTRO DURO): 31 trades | PF 0.900 | -1.7R  ← PERDEDOR
- **Veredicto:** la migración event-driven es SEGURA y mejora el edge (A' 1.511 > baseline 1.424). El POI HTF como filtro duro destruye el edge (A'' 0.900) → queda DESACTIVADO; su rol real (bonus de quality_score, no gate) es el siguiente paso.

### Libro 21 POI (ontología → biblioteca → código) — 2026-07-15
- Escrito `docs/ict/21_POI.md` tras investigación en fuentes ICT reales (InnerCircleTrader PD Array Matrix, ictkillzone.com, fxopen, tradingstrategyguides).
- Definición canónica: POI = **PD Array en zona correcta (discount/premium) + alineado con sesgo HTF + creado por flujo institucional real**; ROL, no tipo; jerarquía por TIERS (BPR > OB/FVG > breaker); elevado por STACKING multi-TF (OB M15 dentro de FVG H1 = POI T1 apilado).
- **Corrección a nuestra interpretación previa:** el POI NO es exclusivo de HTF. Vive en la ZONA del ITF (M15 intradía); el stacking multi-TF lo eleva. Eso explica por qué forzar "POI HTF como filtro duro" daba PF 0.900.
- Tesis `20_TESIS_ICT.md` actualizada (§5b POI: rol, tiers, stacking, ancla narrativa, BONUS no filtro duro). Índice `00_INDICE.md` actualizado.
- Auditoría empírica (`scripts/auditoria_poi.py`, 10.669 zonas medidas): el POI actual del código detecta "cualquier FVG/OB en ventana" SIN narrativa → 100% sin respaldo estructural HTF. El código aún marca POI sin anclarlo a su BOS/CHOCH. Falta: anclar POI a narrativa en el código.

---

## 3. Hitos y Objetivos

| ID | Objetivo | Descripción | Estado | Prioridad |
|----|----------|-------------|--------|-----------|
| A1 | Actualizar documentación | README, AGENT_ARCHITECTURE, harness | ✅ | Alta |
| A2 | Parameter Tuning (F12) | Optuna integrado | ✅ | Alta |
| A4 | Stochastic Exhaustion (F10) | Wyckoff agent | ✅ | Alta |
| A5 | Tests + cobertura | 6 módulos + harness | ✅ | Alta |
| A7 | Validación cuantitativa (F9/F13) | PurgedKFold, CVaR, DSR, PBO | ✅ | Alta |
| A9 | Plan mejora estrategia | ML off, symbol breakdown | ✅ | Alta |
| A10 | Edge Diagnosis (21×8) | 168/168 celdas | ✅ | Alta |
| A11 | Arranque automático FundedNext | PowerShell + mutex + loop | ✅ | Alta |
| A3 | Discrepancia Harness | 11 adapters documentados | ✅ | Media |
| A6 | Expandir datos | >3-4 años históricos (EURUSD/XAUUSD M15 OK; 5 majors sin M5/M1) | 🟢 **Suficiente para A12** (2026-07-24) | Alta |
| **R0** | **Contratos PO3 congelados** | A/M/D aprobados (libro 08) | ✅ | Alta |
| **R1** | **Capa de estado PO3** | `po3_state`, tests, UI | ✅ | Alta |
| **R2** | **Killzones + TZ unificadas** | UTC canónico, helper único | ✅ | Alta |
| **R3** | **Huecos arquitectura (liquidez, open día, CHOCH-gate)** | canonical_sweep, PO3-2 | ✅ | Alta |
| **R3.5** | **Huecos canon ICT en tesis (SMT/Breaker/OTE)** | Libros 14-17/20 hechos; **21 (POI) ✅, 22 (Breaker/MMXM) 🟢 REPARADO (2026-07-22), 23 (OTE) 🟢, 25 (SMT Divergence) 🟢 CERRADO (2026-07-23): detector `smt_divergence()` + `flag_smt_divergence()` (compara 2 swing pts por instrumento, V-shape anti-lookahead); 13/13 tests GREEN; wired a canonical.py (post-signal flag, same pattern as breaker/ote); book `25_SMT_DIVERGENCIA.md`; index updated. 0 gaps restantes** | 🟢 **CERRADO** | Alta |
| **R4** | **Auditoría + medición ICT puro** | Look-ahead ✅; SB/PO3 sin edge; **Turtle v2.8 + funding-gate 6m: REJECT_NO_EDGE** (informe 2026-07-17) | ✅ Cerrado | Alta |
| **R4-tesis** | **Tesis ejecución óptima (libro 18)** | 3 capas + SL/entry exec TF + RR 1:3 | ✅ | Alta |
| A12 | Walk-forward OOS celda ganadora | `no_session`×XAUUSD: 1er pase falló (PF -0.058, N bajo). **Data multi-año YA disponible** — pendiente re-run post-R4/R6 limpio (costs ON). **NO bloqueado por parquet** | 🔴 Pendiente (re-run) | Alta |
| A8 | Deployment Guide (F8) | VPS, systemd/NSSM | 🔴 Pendiente | Baja |
| **R5** | **Datos A6 (umbral M15 multi-año)** | ≥3-4 años M15 XAUUSD/EURUSD | ✅ **CERRADO en disco (2026-07-24)** — XAUUSD_M15 ~4.54y, EURUSD_M15 ~4.56y. Ver `docs/DATA_STATUS.md`. **No reabrir por docs históricos.** | Alta |
| **R6** | **Backtest profesional (reloj/fill/costos)** | G1 closed-only ✅; G2 fill ✅; G3 costs ✅; R6.4 M2: EURUSD M15 PF=-4.89 (GATE NO PASA); **v2 mtf D1→H4→H1→M15 corrido 2026-07-17 (7 majors, costos ON, OOS 0.3): 0-4 trades/símbolo, ninguno pasa gate, coverage 86.1% (C06 POI missing)** — ⚠️ AUDITADO 2026-07-17: fallas (v2 sin versionar, edge_diagnosis, DSR/PBO, XAUUSD excluido **en esa fecha**). **AUDITORÍA FORMAL 2026-07-23:** sello v1 COMPLETO; **Falla 4 (XAUUSD M15 ausente) RESUELTA en datos 2026-07-24**. Deudas abiertas: versionar `ict_backtest/v2/` (auth Ruben). Re-baseline post-v2.8 EURUSD PF 1.155 (258 trades, costs ON) — primera señal positiva, falta WF multi-fold **A12** | 🔶 **Docs ✅ / Sello v1 ✅ / Veredicto 🔴 pendiente (A12, no R5 data)** | Alta |
| **R9** | **Migración del motor a MarketObject (R9)** | ✅ COMPLETADO (2026-07-15). Representación canónica MarketObject; sequence 100% migrado; equivalencia 15/15 tests; compatibilidad vía adapter intacta. NO incluía eliminar engine.py (deuda R7). | ✅ Cerrado | Alta |
| **R7** | **Unificar motor de decisión (single source of truth)** | **COMPLETADO (2026-07-19):** motores legacy `detectors/bos.py`/`choch.py`/`trend.py` ELIMINADOS (`git rm`); única fuente de verdad `ict_backtest/market_structure.py` (canónico). `signals/pipeline.py`, `ict_backtest/data_feed.py`, `translation.py`, `sequence.py` y scripts migrados a canónico. Tests de producción reescritos; tests legacy movidos a `legacy/tests/`. Migración validada por diagnóstico por etapas (`scripts/diag_etapas.py`: run_sequence genera señales). Diseño de temporalidades/FSM/capacidades en `ARQUITECTURA_TEMPORALIDADES.md` + `ROADMAP_CAPACIDADES.md`. | ✅ Cerrado | Alta |
| **Fase 1 — Migración ATR → RANGO (volatilidad sin indicadores)** | **Eliminar ATR (Wilder) del motor; única fuente de volatilidad = avg_candle_range (rango high-low puro)** | 🟢 **CERRADA (2026-07-20)**: `ict_backtest/_util.avg_candle_range` (Wilder suavizado → media móvil high-low, matemática pura). Migrados: `canonical` (fallback SL bos±0.5→rango), `engine` (STRUCT_SL_MAX_ATR/BUFFER_ATR→RANGE; SL estructural usa rango), `optimize` (SL de señales usa rango), `market_structure` (ELIMINADO `_atr`/Wilder + `atr_period` + caducidad "aged" muerta), `data_feed` (columna contrato `atr` ahora = avg_candle_range; tol BPR 0.3→rango), `detectors/displacement` (body vs rango promedio, ya NO escribe columna `atr`), `plot_equity_curve` (banda rango), `sequence` (fallback zona bos±0.5→rango). Test `test_turtle_thesis_alignment` (STRUCT_SL_MAX_RANGE) + legacy test_detectors adaptados. **Criterio de salida (Fase 2): medir impacto de múltiplo equivalente en backtest vs baseline ATR; si mejora/empata, dejar; si degrada, recalibrar.** | ✅ Cerrada | Alta |
| **Fase 1 (Plan)** | **Arquitectura de Plan — FSM central + emisores D1/H4/H1** | 🟢 ANDANDO (2026-07-19): `ict_backtest/plan_fsm.py` (PlanFSM reductor puro) + `ict_backtest/plan_emitters.py` (emit_d1/emit_h4/emit_h1, funciones puras) + 11 tests TDD verdes + `scripts/fase1_demo_plan.py` (demo sintética: D1+H4+H1→ZONE_ARMED; H1 veto→NO_TRADE). NO toca producción. Falta cablear loop driver a `run_backtest.py` (Fase 1 nivel 2, con datos reales). Ver `docs/plan/ROADMAP_CAPACIDADES.md`. | 🟢 Andando | Alta |
| **Fase 2 (Setup)** | **Arquitectura de Setup — emisor M15 envuelve run_sequence** | 🟢 ANDANDO (2026-07-19): `emit_m15` en `ict_backtest/plan_emitters.py` traduce la salida de `run_sequence` (phase_log) a SETUP_LIVE (BOS_DONE) / STRUCTURE_OK (ENTRY) / None (sin setup). 4 tests TDD verdes + `scripts/fase2_demo_plan.py` (cascada D1→H4→H1→M15: ZONE_ARMED→SETUP_LIVE→STRUCTURE_OK; sin setup se queda en ZONE_ARMED). Bug reparado: FSM ahora permite ZONE_ARMED→STRUCTURE_OK directo (STRUCTURE_OK superset de SETUP_LIVE). 15 tests totales Fase 1+2 verdes. NO toca producción. Falta loop driver real que llame run_sequence y pase su salida a emit_m15 (Fase 2 nivel 2). | 🟢 Andando | Alta |
| **Fase 3 (Ejecucion)** | **Arquitectura de Ejecucion — emisor M5 -> ENTRY_READY** | 🟢 ANDANDO (2026-07-19): `emit_m5` en `ict_backtest/plan_emitters.py` decide SI entrar (no la direccion del plan). ENTRY_READY si M5 confirma en la MISMA direccion; None si no confirma o intenta invertir (plan queda en STRUCTURE_OK). 4 tests TDD verdes + `scripts/fase3_demo_plan.py` (cascada completa D1→H4→H1→M15→M5: ENTRY_READY alcanzado; M5 que rechaza/invierte no rompe el plan). 19 tests totales F1+F2+F3 verdes. NO toca produccion. Cierra el flujo decisional Plan→Setup→Ejecucion. Falta loop driver real (Fase 3 nivel 2) y cablear M1 (Fase 4). | 🟢 Andando | Alta |
| **Fase 4 (Optimizacion)** | **Arquitectura de Precision — emisor M1 -> IN_TRADE** | 🟢 ANDANDO (2026-07-19): `emit_m1` en `ict_backtest/plan_emitters.py` es el ultimo filtro de timing (SB fino). IN_TRADE si M1 confirma en la MISMA direccion; None si no (plan queda en ENTRY_READY). `PlanVerdict.IN_TRADE` + transicion ENTRY_READY->IN_TRADE en `plan_fsm.py`. 4 tests TDD verdes + `scripts/fase4_demo_plan.py` (cascada COMPLETA NO_TRADE->CONTEXT_OK->ZONE_ARMED->SETUP_LIVE/STRUCTURE_OK->ENTRY_READY->IN_TRADE). 23 tests F1+F2+F3+F4 verdes. M1 NO cambia el edge, solo optimiza timing/spread/SL/RR. **CRITERIO DE SALIDA (bench, no FSM): M1 debe demostrar mejora estadistica vs M5; si no, M1 no pasa** (evaluar con backtest comparativo M5 vs M1). NO toca produccion. | 🟢 Andando | Alta |
| **A1 Nivel 2 (loop driver FSM → ejecución)** | **Cablear PlanFSM al loop de `run_backtest` como compuerta de EJECUCIÓN** | 🟢 **CERRADA Opción B (2026-07-20, TDD RED→GREEN→Demo→Verify)**. `run_sequence` INTACTO; `run_sequence_backtest` gana kwarg `plan_gate=False` (default → histórico intacto). La FSM gobierna la EJECUCIÓN de trades, NO la generación de señales. Umbral inicial `STRUCTURE_OK`. `plan_step`/`run_plan_fsm` en `plan_driver` (reusan `_objs_before` + `emit_*`); `_field` en `plan_emitters` para aceptar dict O `ICTSignal`. Tests `tests/test_plan_gate_a1.py` (4, call site real) + demo `scripts/plan_gate_demo.py`. AC cumplidos: mismo nº señales / solo cambia nº trades / cada veto con estado. 25/25 suite `plan_*`. Subir a `ENTRY_READY` = Fase 2 de A1 (kwarg umbral). Ver `docs/plan/AUDITORIA_TESIS_FASE5.md` §9. | ✅ Cerrada | Alta |
| **Fase 1 — Lectura multitemporal (causa raíz A1)** | **Resolver la infraestructura de lectura multitemporal: 6 TF D1→H4→H1→M15→M5→M1 llegan al motor vía MultiTFContext closed-only; `run_sequence` recibe el contexto y decide con el MISMO HTF de hoy (Opción A)** | 🟢 **CERRADA (2026-07-20, TDD RED→GREEN + call site real + verify empírico)**. Causa raíz real: el motor usaba interfaz de 1-HTF (`canonical.py` `est_htf_fn` leía `ms.get(htf)`), NO falta de datos (los 6 TF YA estaban en disco: `data/raw/*_{D1,H4,H1,M15,M5,M1}.parquet`). Cambios: nuevo `ict_backtest/multitf_context.py` (`MultiTFContext` + `build_multitf_context` que reusa `v2/context_mtf.build_context_stack`, closed-only anti look-ahead) + `extract_htf_layer`; `canonical.evaluate_signals` carga TF_CHAIN completa y pasa `est_htf_ctx_fn` a `run_sequence`; `run_sequence` acepta `est_htf_ctx_fn`+`htf` y reduce vía `extract_htf_layer(context, htf)` (Opción A: 100% idéntico al baseline de 1 nivel). `run_sequence` NO cambia su lógica interna (blast radius mínimo). Evidencia: (1) 6 TF llegan ✓; (2) sin look-ahead (`build_context_stack` via `closed_row_at_time`) ✓; (3) `run_sequence` recibe `MultiTFContext` (test call site real) ✓; (4) idéntico al baseline (tests sintéticos + `scripts/fase1_verify.py` sobre EURUSD real, entry_at iguales) ✓; (5) tests verdes ✓; (6) doc ✓. Los otros 5 TF viajan disponibles pero NO influyen (Fase 2 decidirá cómo aprovecharlos). Ver `docs/plan/AUDITORIA_TESIS_FASE5.md` §10. | ✅ Cerrada | Alta |
| **C (Fase C, capa autoridad zonas)** | **Capa de percepción de autoridad de zonas HTF (C0–C4, TDD)** | ✅ DONE 2026-07-18: `ict_backtest/htf_pd_index.py` (plumbing HTF O(n) closed-only) + `ict_backtest/zone_authority.py` (evaluador, lee-no-crea) + enchufe en `canonical.est_htf_fn`/`sequence.run_sequence`. 16 tests verdes. **NO altera conteo de señales (R1)**; NO crea zonas; NO es gate duro; respeta Contrato de no invasión. Cierra root cause A'' (POI anclado HTF muerto por plumbing, no diseño). Ver `docs/plan/ETAPA_4_FASE_C_PLAN.md`. **Cableada en producción (observador + backtests CLI/v2) 2026-07-18** — ver `test_fase_c_production_wiring.py` + `test_fase_d_paso1_backtest_wiring.py`. | ✅ Cerrado | Alta |
| **D (Fase D, Diagnosis Engine)** | **Infraestructura de autoexplicación post-backtest** | 🔶 DISEÑO APROBADO 2026-07-18 (refinado con 4 reglas de Ruben): el sistema explica POR QUÉ ganó/perdió cuando el PF no alcanza, SIN optimizar params (no es Optuna). Cadena: TradeContext(congelado)→Statistics→Correlation Engine→Hypothesis Generator→Evidence Ranking→Final Report. **Reglas de hierro:** (1) `TradeContext` @frozen INMUTABLE, 1 vez, nunca post-outcome (anti sesgo retrospectiva = look-ahead R4); (2) ids `backtest_id`+`trade_id`+`signal_id`+`context_version`+`context_created_at`; (3) Correlation Engine SEPARADO de Hypothesis Engine; (4) Final Report incluye OBLIGATORIAMENTE "¿qué NO puede concluir?" (anti-sobreajuste). **Separación (Ruben): `simulate_trade` SIMULA; `simulate_trade_with_context` EMITE RawDiagnosticData; `diagnostics.context_builder` CONGELA TradeContext (engine.py queda limpio).** **Paso 1 (Fase C en backtests, METADATA, R1 OK) ✅; Paso 2 (TradeContext emitido+congelado, R1 PnL idéntico, inmutable, ids) ✅ 2026-07-18.** **Paso 2b — MIGRACIÓN MULTI-TF (2026-07-18, reglas #1/#4/#5/#7 de Ruben): el expediente ahora trae la CADENA COMPLETA D1/H4/H1/M15/M5/M1 (observabilidad pura, NO cambia la decisión R7).** Reusa `ict_backtest/v2/context_mtf.py` (closed-only anti look-ahead, ya existente y deshabilitado). Cambios: `run_sequence_backtest` carga `TF_CHAIN=(D1,H4,H1,M15,M5,M1)` + emite `market_stack` por señal; nuevo `diagnostics/mtf_context.py` normaliza a `MarketContextFrame` por rol (D1 bias/premium_discount · H4 poi · H1 liquidity · M15 setup/sweep/displacement/bos/fvg/ob · M5/M1 confirmation/execution); `TradeContext` v2 (`ctx-2.0`) suma `market_context` (v1 intacta, inmutable). TF ausente => `available=False`/`MISSING` (nada inventado). 11 tests verdes (`test_fase_d_mtf_context.py`+`test_fase_d_mtf_wiring.py`). Validación 6m EURUSD: 36 trades, 6 TF 100% disponibilidad, 0 incompletos, D1.premium_discount y H4.poi con datos reales (antes UNKNOWN). Ver `docs/plan/ETAPA_DIAGNOSIS_ENGINE_MTF.md`. **Fase E ✅ CERRADA 2026-07-18** (ver fila E). | 🔶 Diseño ✅ / Paso 1 ✅ / Paso 2 ✅ / 2b multi-TF ✅ / E ✅ CERRADA | Alta |
| **E (Fase E, Motores de Análisis)** | **Statistics / Correlation / Hypothesis engines (solo lectura)** | ✅ CERRADA 2026-07-18 (E1–E4, TDD, 23 tests). **Diseño:** `docs/plan/ETAPA_DIAGNOSIS_ENGINE_FASE_E.md`. **Reglas de arquitectura:** (1) 3 módulos INDEPENDIENTES en `ict_backtest/diagnostics/`; (2) solo CONSUMEN TradeContext v2 / `market_context`, no lo mutan; (3) NO modifican motor de entradas ni R7 (engine/sequence/canonical intactos); (4) NO introducen lógica de trading; (5) análisis antes que optimización (anti edge-hunting: no eligen "mejor" cohorte). **Módulos:** `cohorts.py` (facetas puras, anti look-ahead, leen contexto congelado), `statistics_engine.py` (StatisticsReport: overall + cohorts con Wilson IC95 + comparisons, n y `can_conclude`), `correlation_engine.py` (CorrelationReport: phi / punto-biserial por faceta, `strength`, `can_conclude`), `hypothesis_engine.py` (HypothesisReport: consume los 2 reportes, hipótesis explícitas con evidencia/n/métricas/confianza, `inconclusive[]`), `diagnosis_report.py` (orquestador puro: cadena stats→corr→hypo, sin lógica propia). **E4 corrida real EURUSD 6m (36 trades):** todas las cohortes `can_conclude=False` (n<30 por categoría); única señal tenue M5 confirmación coef +0.27 (small). Conclusión honesta del motor: NO hay edge concluyente con n=36; falta muestra (walk-forward más largo / más pares). Ver `scripts/fase_e_run_real_e4.py`. | ✅ Cerrada | Alta |

| **Fase 0 — Formalización (SPEC firmada) + MDS** | **Firmar SPEC_TESIS_FORMAL como CONTRATO FUENTE y crear diseños de módulo (R1/R2)** | 🟢 **CERRADA (2026-07-20)**: `docs/ict/SPEC_TESIS_FORMAL.md` FIRMADA (era DRAFT; excepción DEC-009g registra que B1/C0-C4/A1 Nivel 2 se hicieron antes sin firma, ya validados). Creados `docs/specs/` con 8 MDS nuevos (B2 exec M5/M1, B3 internal/external liq, Killzones L/NY PM, C2 Silver Bullet, C3 Turtle Soup, D1 OTE, E1 Trade Mgmt, RR por setup) + `INDICE_MDS.md`. Cubre los OBLIGATORIOS ❌ de la matriz §9. Sincronía SPEC↔MDS (R2) restablecida. Backtest PF sigue bloqueado hasta Fase G (R4). Orden de impl: B2→B3→C2→C3→D1→E1 (ROADMAP §4). Ver `docs/plan/PROPUESTA_BRECHA_A1_CABLEADO_TOPDOWN.md` (Brecha A1 real: cablear `top_down_allows_trade` al canónico como filtro, NO 2do cerebro). | ✅ Cerrada | Alta |
| **Brecha A1 real — 3 capas en motor** | **run_sequence filtra por cascada D1→H4→H1 (no solo H4)** | 🟢 **CERRADA (2026-07-20, TDD RED→GREEN + call site real + verify empírico):** `top_down_allows_trade` (v2/context_mtf.py:136) cableado en `run_sequence` (sequence.py ~l.399-417) como filtro Opción B (anota `htf_aligned`/`htf_reason`, veta si la dirección choca con D1/H4/H1; `require_pd=False`). El motor DEJA de decidir solo en H4: la cascada D1→H4→H1 ahora gobierna la dirección. `htf_poi_fn` NO se usa como gate duro (PF 0.900). Tests `tests/test_a1_topdown_filter.py` (6 passed) + verify CASO A (D1 RANGING → 0 señales en pipeline real). Ver DEC-009i (Ruben elige C: PlanFSM → cerebro de dirección, modifica DEC-009f). | 🟢 Cerrada | Alta |
| **Fase B2 — exec TF M5/M1** | **entry/SL/TP anclados al TF de ejecución fino (M5/M1), no al LTF (M15)** | 🟢 **CERRADA (2026-07-20, TDD RED→GREEN + call site real):** `evaluate_signals` gana kwarg `exec_tf` (default None = M15, regresión cero). Si `exec_tf=M5/M1` (ya cargado en `ms`), recalcula entry/SL/TP/liq/killzone sobre esa vela fina (SL SIEMPRE en exec TF, libro 18). M15 sigue siendo LTF (detección de SETUP) y default de exec_tf. Nuevo helper `_exec_idx_at_time` (mapeo anti look-ahead closed-only). Tests `tests/test_b2_exec_tf.py` (4 passed). MDS: `docs/specs/MDS_B2_EXEC_M5_M1.md`. | 🟢 Cerrada | Alta |
| **Fase 1.1 (auditoría) — wiring exec_tf en el runner** | **`exec_tf` debe viajar desde `run_backtest.py` (CLI + wrappers) hasta `canonical.evaluate_signals`** | 🟢 **CERRADA (2026-07-22, wiring + test de call-site real):** la auditoría técnica detectó que `evaluate_signals` YA consumía `exec_tf` pero el runner nunca lo pasaba (0 matches de `exec_tf=` en `run_backtest.py`). Propagado en 4 puntos: firma de `run_sequence_backtest` + llamada interna a `generate_sequence_signals`; firma de `generate_sequence_signals`; wrapper `run()`; y CLI `--exec-tf` en `main()`. Regresión cero: `exec_tf=None` (= ltf) preserva comportamiento histórico. Test `tests/test_b2_exec_tf_wiring.py` (4 passed, call-site real con monkeypatch de `load_frames` + `evaluate_signals`, sin I/O de datos). Cierra el ítem "Exec TF M5 pendiente" de `AUDITORIA_DOCUMENTAL_2026-07-21.md`. | 🟢 Cerrada | Alta |
| **Fase 1.2 (auditoría) — wiring trade_mgmt en el simulador** | **`trade_mgmt.apply_trade_management` (BE+parcial+trailing) debe invocarse desde `simulate_trade` cuando está habilitado** | 🟢 **CERRADA (2026-07-22, wiring + test de call-site real):** la auditoría técnica detectó que `simulate_trade` (engine.py) NUNCA invocaba `apply_trade_management` (wiring muerto; DEC-009l lo afirmaba erróneamente como ya aplicado). Se añadió `trade_mgmt: bool = False` a `simulate_trade` y, si True, re-simula el trade con BE+parcial+trailing vía `apply_trade_management` (mapea exit_reason a TP/SL/BE/TRAILING/hold_limit, PnL ponderado). Propagado a `simulate_trade_with_context` → `run_sequence_backtest`/`run`; CLI `--trade-mgmt` en `run_backtest.py`. Regresión cero: `trade_mgmt=False` (= histórico SL/TP simples). Test `tests/test_fase1_2_trade_mgmt_wiring.py` (6: call-site real + BE/partial/trailing + regresión histórica). Ver también fila E1 arriba. | 🟢 Cerrada | Alta |
| **Dashboard Fase 0.1 — Wyckoff completo (`fase_wyckoff_m15`) — 2026-07-22** | **Tras Fase 0 la lectura top-down vive, pero la Fase Wyckoff salía "(no disponible): StructureConfig got unexpected keyword 'atr_period'"** | 🟢 **CERRADA (2026-07-22, TDD RED→GREEN + datos reales):** causa = `fase_wyckoff_m15.py:50` pasaba `atr_period=14` a `StructureConfig`, parámetro ELIMINADO en la Migración ATR→RANGO (Fase 1). Borrado el parámetro sobrante; `StructureConfig(swing_lookback=5, confirm_bars=2)` moderna. WyckoffAgent + indicators (add_atr/add_stochastic) ya estaban completos (no había lógica faltante). Verificado: 2 tests `tests/test_fase0_1_wyckoff_wiring.py` PASSED (fase real en set conocido + bias/confianza); `rutina_eurusd.py` completo ahora pinta Fase Wyckoff viva: D1=MARKDOWN(BEARISH 60%) / H4=MARKUP(BULLISH 55%) / M15=MARKDOWN(BEARISH 55%), ALINEACION=EN CONFLICTO (precaución). MARKET STATE que pidió Ruben YA FUNCIONA. NOTA deuda fuera de alcance: `atr_period=14` sobrante TAMBIÉN en `ablation_study.py`, `build_real_dataset.py`, `fase_g_equivalencia.py`, `gen_synth_ml.py` (scripts de análisis/backtest/synth, no del motor del dashboard) — no tocados por esta Fase 0.1. | 🟢 Cerrada | Media |
| **Dashboard Fase 0 — revivir motor (re-wire `rutina_eurusd`) — 2026-07-22** | **`engine.run_cycle` devolvía dict vacío por import roto: `rutina_eurusd` usaba detectores legacy eliminados (`detectors.bos/choch/trend`)** | 🟢 **CERRADA (2026-07-22, TDD RED→GREEN + datos reales):** causa raíz = `detectors/bos.py`, `choch.py`, `trend.py` fueron BORRADOS en un refactor; `rutina_eurusd.analyze_timeframe` los importaba → `ImportError: cannot import name 'BosConfig'`. Re-wire a librerías de cálculo puras COMPARTIDAS (no pipeline backtest, respeta separación Ruben): `ict_backtest.market_structure.detect_market_structure` (BOS/CHOCH/trend/swing en una pasada, mismo contrato de columnas) + `detectors.liquidity_context.canonical_sweep` (sweep, sin look-ahead). OB/FVG/ZONAS se mantienen en sus detectores actuales. Verificado: `analyze_timeframe` sobre EURUSD_M15 real devuelve trend/choch/zone/OTE vivos; 3 tests `tests/test_rutina_eurusd_wiring.py` PASSED (fresh: 3 passed in 1.10s). `engine.run_cycle` completo pesado en entorno sin venv pero YA NO aborta en import. LECTURA REAL 2026-07-22 21:30 UTC: D1 BAJISTA (LL, DISCOUNT) / H4 ALCISTA (HL, PREMIUM, FVG bullish) / M15 BAJISTA (LL, CHOCH_BEARISH invalidated, OB bearish, DISCOUNT). Veredicto SHORT (L1/S2, contexto mixto H4 alcista). Plan SHORT OTE 1.14099 / SL 1.14186 (8.7p) / TP 1.14062 (3.7p) → R:R 1:0.42 DESCARTAR (no da 1:2). Motor honesto: hay sesgo pero no setup con R:R decente hoy. | 🟢 Cerrada | Alta |
| **Auditoría Dashboard Operacional (`app_observador/`) — 2026-07-22** | **Auditar Dashboard vs tesis; verificar separación backtest≠dashboard; identificar faltantes** | 🟢 **COMPLETADA (2026-07-22, auditoría de código, sin cambios):** el Dashboard operacional es `app_observador/` (PySide6, 6 tabs). Confirma SEPARACIÓN ESTRICTA: su motor `engine.run_cycle` orquesta scripts MT5 en vivo (`rutina_eurusd`, `semaforo_fundednext`, `mapa_precio`, `fase_wyckoff_m15`) + plan canónico R7 live; NO importa `run_backtest` (directiva Ruben: backtest=laboratorio, dashboard=panel operacional). Cobertura tesis alta (bias/estructura/BOS/CHOCH/sweep/OTE/killzone/Turtle Soup/Silver Bullet/PO3/riesgo/checklist ✅). FALTAN: SMT Divergence (Opción B), Régimen de mercado, Strength Score, ranking multi-activo. PARCIAL (corregido tras Fase 0): Premium/Discount + OTE YA estaban en `compute_zones` (zones.py) y se muestran al revivir el motor; FVG/OB solo texto+mapa, no widget dedicado; confianza/calidad solo semáforo. Regla diseño Ruben: campo ausente → "EN CONSTRUCCIÓN", nunca forzar. Régimen se deriva de `avg_candle_range` (rango puro, SIN ATR). 🚩 Bandera amarilla: `engine.py:202-207` (`_canonical_plan`) fuerza `votes[LONG]=2` si el plan canónico es LONG → crea falso consenso; corregir antes de ampliar el dashboard. Roadmap Fase A–G (A=cabecera always-on ALTA, B=widget Calidad/Confianza ALTA, B2=panel MARKET STATE, C=SMT, D=Premium/Discount YA EN ZONES, E=Régimen MEDIA, F=ranking BAJA, G=dockable BAJA). Informe: `docs/auditorias/AUDITORIA_DASHBOARD_OPERACIONAL_2026-07-22.md`. | 🟢 Cerrada | Alta |
| **Auditoría Dashboard (monitoring) — 2026-07-22** | **Mapear módulos `monitoring/`, flujo backtest→dashboard, componentes muertos/desacoplados** | 🟢 **COMPLETADA (2026-07-22, auditoría de código, sin cambios):** hallazgo central: el "dashboard" NO es UI ni está cableado al backtest; es `generate_dashboard()` (dict) de MONITOREO EN VIVO (paper/live) que consume equity/trades de un feed en vivo vía `legacy/monitoring/harness_adapter.py` (MonitoringHarnessAdapter) o `paper_trading/runner.py` (solo DriftDetector). El backtest (`run_backtest.py`) tiene SU PROPIO cálculo de métricas (`_metrics()` en R, `diagnostics/statistics_engine.py`) + `plot_equity_curve.py` (PNG) que el dashboard IGNORA → DUPLICACIÓN de lógica de métricas (Sharpe/Sortino/Calmar/PF/DD en 3 lugares, distintas unidades R vs %). Componentes muertos: `config.py` tiene 7 campos `alert_*` SIN alerter que los use; `result["alerts"]=[]` nunca se llena; `AUDIT_REPORT.md` (F15) describe un alerter/governance/orchestration que NO existen en `monitoring/` hoy (deuda documental). `generate_dashboard` recibe `drift` pero solo crea `{"features": {}}` vacío (el PSI nunca se puebla en el dashboard). Informe completo: `docs/auditorias/AUDITORIA_DASHBOARD_2026-07-22.md`. Recomendación: NO reescribir; conectar `run_backtest`→`generate_dashboard` reusando `statistics_engine` para unificar métricas. | 🟢 Cerrada | Alta |
| **Brecha A — POI anclado (bonus, no gate)** | **htf_poi_fn REAL consume POI anclado HTF y lo anota como bonus** | 🟢 **CERRADA (2026-07-20, TDD RED→GREEN + call site real):** hook `htf_poi_fn` en `sequence.py:403` (hasta hoy None = muerto) ahora recibe `make_htf_poi_fn(htf_pd_index, ltf_map)` desde `canonical.evaluate_signals` cuando `enable_pd_index=True` (as_gate=False → nunca veta, solo anota `poi_present`). `ICTSignal.poi_present` nuevo en `engine.py`. Nuevo `ict_backtest/poi_filter.py`. REGRESIÓN: `enable_pd_index=False` → `htf_poi_fn=None` → idéntico histórico. Tests `tests/test_a_poi_anchored.py` (12 passed). Rol bonus respeta auditoría Fase E (gate duro = PF 0.900). | 🟢 Cerrada | Alta |
| **B3 — Liquidez internal vs external** | **TP primario = liquidez internal (BSL/SSL); objetivo macro = external (PDH/PDL día previo)** | 🟢 **CERRADA (2026-07-20, TDD RED→GREEN + call site real):** `_tp_liquidity` (engine.py) ahora devuelve `dict{internal, external}`; `internal`=bsl_price/ssl_price del row (idéntico a histórico), `external`=PDH/PDL del día previo en el `df` (calculado, antes no existía). Call-site en `canonical.evaluate_signals` usa `internal` como TP primario (regresión cero: antes también usaba bsl/ssl) y anota `external_tp` en `ICTSignal` (metadato para E1). Sin `df`/día previo → external=None → comportamiento histórico. Tests `tests/test_b3_tp_hierarchy.py` (5: unidad + aceptación call-site real con 2 días, TP=1.1300 internal / external_tp=1.1100 PDH). MDS: `docs/specs/MDS_B3_LIQUIDEZ_INT_EXT.md`. **⚠️ NOTA DE DEUDA (ver AUDITORIA_COMPARATIVA_2026-07-25 / AUDITORIA_FIDELIDAD_TESIS):** 🟢 = `_tp_liquidity` devuelve `internal`+`external_tp` metadato en `ICTSignal`. PERO el TP primario real sigue usando cluster promedio HTF (bug#2, tesis §13 ❌), no swing opuesto LTF más cercano. `external_tp` es solo metadato. Deuda ROJA #10 / AMARILLA. | 🟢 Cerrada | Alta |
| **C2 — Silver Bullet (SB)** | **Setup de 'hora limpia': sweep+retorno DENTRO de London Open / NY AM** | 🟢 **CERRADA (2026-07-20, TDD RED→GREEN + call site real del orquestador):** `ict_backtest/setups/silver_bullet.py` con `is_silver_bullet` + `flag_silver_bullet`. Cableado en `canonical.evaluate_signals` como PASO POST (knob apagado: solo anota `sb_confirmed`/`sb_killzone`, NO filtra duro — principio Brecha D). Reusa `killzone_en` (KZ-2 ya corregido). Tests `tests/test_c2_silver_bullet.py` (11) + aceptación orquestador (`test_orchestrator_setups_wired.py`: SB='L' en London Open). MDS: `docs/specs/MDS_C2_SILVER_BULLET.md`. **⚠️ NOTA DE DEUDA (ver AUDITORIA_COMPARATIVA_2026-07-25 / AUDITORIA_FIDELIDAD_TESIS):** 🟢 = módulo cableado como PASO POST (anota `sb_confirmed`/`sb_killzone`, knob apagado, NO filtra duro). Regla de tesis §17 NO ejecutada en el edge: el flag SB no veta señales ni aplica RR 1:2 en producción. Deuda AMARILLA #6. | 🟢 Cerrada | Alta |
| **C3 — Turtle Soup** | **Sweep PDH/PDL día previo + reversión (stop-hunt que falla)** | 🟢 **CERRADA (2026-07-20, TDD RED→GREEN + call site real del orquestador):** `ict_backtest/setups/turtle_soup.py` con `is_turtle_soup` + `flag_turtle_soup`. Cableado en `evaluate_signals` (PASO POST, knob apagado: anota `turtle_confirmed`/`turtle_broke`, no filtra). PDH/PDL del día previo desde `frames[ltf]`. Tests `tests/test_c3_turtle_soup.py` (6) + aceptación orquestador. MDS: `docs/specs/MDS_C3_TURTLE_SOUP.md`. **⚠️ NOTA DE DEUDA (ver AUDITORIA_COMPARATIVA_2026-07-25 / AUDITORIA_FIDELIDAD_TESIS):** 🟢 = módulo cableado como PASO POST (anota `turtle_confirmed`/`turtle_broke`, knob apagado, NO filtra duro). Regla de tesis §18 NO ejecutada en el edge: Turtle Soup no cambia señales ni edge. Deuda AMARILLA #7. | 🟢 Cerrada | Alta |
| **D1 — OTE (Optimal Trade Entry)** | **Entry en retrace 62-79% Fib de la pierna (no solo 'retorno a FVG')** | 🟢 **CERRADA (2026-07-20, TDD RED→GREEN + call site real del orquestador):** `ict_backtest/setups/ote.py` con `ote_zone`/`is_ote_entry`/`flag_ote`. Cableado en `evaluate_signals` (PASO POST, knob apagado: anota `ote_confirmed`/`ote_zone`, no filtra). Lee swing del row de entry. Tests `tests/test_d1_ote.py` (10) + aceptación orquestador. MDS: `docs/specs/MDS_D1_OTE.md`. **⚠️ NOTA DE DEUDA (ver AUDITORIA_COMPARATIVA_2026-07-25 / AUDITORIA_FIDELIDAD_TESIS):** 🟢 = módulo cableado como PASO POST (anota `ote_confirmed`/`ote_zone`, knob apagado, NO filtra duro). La auditoría de fidelidad lo marca ⚠️ "no-op en producción real". Regla de tesis §21 NO ejecutada en el edge. Deuda AMARILLA #8. | 🟢 Cerrada | Alta |
| **E1 — Trade Management** | **BE, salida parcial, trailing stop (dentro del trade)** | 🟢 **MÓDULO CERRADO (2026-07-20, TDD RED→GREEN):** `ict_backtest/trade_mgmt.py` con `to_breakeven`/`partial_exit`/`trailing_stop` (funciones puras) + `apply_trade_management(entry, sl, tp, direction, df, ...)`. Tests `tests/test_e1_trade_mgmt.py` (19) + `tests/test_e1_applied_trade_mgmt.py` (3: parcial+TP, BE protection, SL directo). MDS: `docs/specs/MDS_E1_TRADE_MANAGEMENT.md`. **CABLEADO AL SIMULADOR (Fase 1.2, 2026-07-22):** la auditoría técnica detectó que `simulate_trade` (engine.py) NUNCA invocaba `apply_trade_management` (wiring muerto; DEC-009l lo afirmaba erróneamente como ya aplicado). Ahora `simulate_trade` acepta `trade_mgmt: bool = False` y, si True, re-simula el trade con BE+parcial+trailing vía `apply_trade_management` (mapea exit_reason a TP/SL/BE/TRAILING/hold_limit). Propagado a `simulate_trade_with_context` → `run_sequence_backtest`/`run`/`generate_sequence_signals` + CLI `--trade-mgmt` en `run_backtest.py`. Regresión cero: `trade_mgmt=False` (= histórico SL/TP simples). Test `tests/test_fase1_2_trade_mgmt_wiring.py` (6: call-site real + BE/partial/trailing + regresión). **⚠️ NOTA DE DEUDA (ver AUDITORIA_COMPARATIVA_2026-07-25 / AUDITORIA_FIDELIDAD_TESIS):** 🟢 = módulo cableado + wiring en `simulate_trade` PERO `trade_mgmt=False` por defecto (regresión cero = SL/TP simples). La gestión BE/parcial/trailing NO se ejecuta en el edge salvo `--trade-mgmt`. Regla de tesis §22 NO ejecutada por defecto. Deuda AMARILLA #9. | 🟢 Cerrada | Alta |
| **RR por setup** | **RR objetivo según setup (SB 1:2, Turtle 1:1.5, OTE 1:3, default 1:3)** | 🟢 **CERRADA + APLICADA (2026-07-20, TDD RED→GREEN + call site real del pipeline):** `ict_backtest/setups/rr_map.py` (`RR_BY_SETUP`/`rr_for`/`flag_rr`) + `_rr_for_raw_signal(s, ltf_df, direction, ltf)` en `canonical.evaluate_signals` que resuelve el setup de la señal CRUDa vía los detectores reales (`is_silver_bullet`/`is_turtle_soup`/`is_ote_entry`) y aplica `rr_target` al TP: `tp = entry +/- rr_target*risk` (cuando no hay liquidez internal). Reemplaza el `3.0*risk` fijo. Regresión cero: la guarda de mínimo 2R solo aplica al fallback de liquidez internal. Tests `tests/test_rr_map.py` (6) + `tests/test_rr_applied_to_tp.py` (4: SB→2.0, Turtle→1.5, OTE→3.0, default→3.0 sobre el TP real). MDS: `docs/specs/MDS_RR_POR_SETUP.md`. **⚠️ NOTA DE DEUDA (ver AUDITORIA_COMPARATIVA_2026-07-25 / AUDITORIA_FIDELIDAD_TESIS):** 🟢 = `rr_target` se aplica al TP SOLO cuando NO hay liquidez internal (casi siempre la hay, ver bug#2). En la práctica el RR 1:3 de tesis §20 NO se ejecuta en el edge; default sigue `fixed2r`=1:2. Deuda ROJA #3. | 🟢 Cerrada | Alta |
| **Killzones L/NY PM (KZ-2)** | **TZ servidor→UTC vía ZoneInfo (sin offset fijo), mata bug NY=-4/LDN=0/TOKYO=+9** | 🟢 **CERRADA (2026-07-20, TDD RED→GREEN):** `detectors/killzones.py` y `ict_backtest/rules.py` (killzone_en) reescritos: `server_to_utc(ts, broker_tz)` localiza vía ZoneInfo y evalúa bandas ET convertidas a UTC POR DÍA (DST automático). Firma `killzone_en(ts, broker_tz=None)` estable (canonical intacto: sin broker_tz asume UTC crudo legacy). NUNCA offset fijo (verificado por grep + test `test_sin_offset_fijo_hardcodeado`). Tests `tests/test_killzones_tz.py` (7: verano vs invierno dan misma killzone). DEC-009i registra el principio. **⚠️ RECONCILIACIÓN 2026-07-26:** el filtro DURO de las 3 killzones SÍ está activo en `ict_backtest/canonical.py` (loop de señales descarta entry fuera de `London Open`/`New York AM`/`New York PM` vía `killzone_en`, KZ-2 con DST). Tesis §15 EJECUTADA en el edge. Los tests OTE que el filtro duro rompió fueron corregidos (escenario sintético ahora en London Open real). **Deuda ROJA #5 → CERRADA.** | 🟢 Cerrada | Alta |
| **R10.C Adaptador + Wiring (Fase F)** | **Adaptador de formato + cableado al pipeline canónico** | 🟢 **CERRADA (2026-07-22, TDD):** `ict_backtest/semantic_adapter.py` con `adapt_semantic_to_legacy()` que traza la cadena causal (SWEEP root + BOS parent) desde el ObjectGraph y produce dicts compatibles con canonical.py. `event_engine.py` gana `_find_return_bar()` + campo `entry_at` + `time` en la salida de `run_semantic`. **WIRED (2026-07-22):** `canonical.evaluate_signals` acepta `use_semantic=True` (DEFAULT) que reemplaza `run_sequence` por `run_semantic + adaptador`. `run_backtest.py` propaga `use_semantic` desde CLI (`--no-use-semantic` para legacy). `sequence.py` se conserva in-situ (47 imports) pero el default ahora usa el motor semántico. Tests `test_r10c_adapter.py` (20) + `test_r10c_semantic_vs_legacy.py` (7) + suite existente (106 key tests verdes). **sequence.py NO se movió** — decisiones de arquitectura: 47 imports, risk de breakage alto. | 🟢 Cerrada | Alta |
| **GEOMETRY — Laboratorio de Geometría del Mercado (GREENFIELD)** | **Descubrir LEYES geométricas invariantes del camino del precio (sin ICT, sin indicadores)** | 🟢 **D1+D2+D3 CERRADOS (2026-07-23, TDD 11 tests verdes + D3 real):** módulo aislado `geometry_lab/` (no toca `ict_backtest/`). D1/D2: fórmulas puras + null-permutación calibrado. **HALLAZGO QUE CORRIGIÓ LA TESIS:** κ de Menger NO invariante de escala (escala 1/λ); el invariante auténtico es el COSENO del ángulo (corregido en `24_GEOMETRIA_DEL_MERCADO.md` y `LAB_GEOMETRICO_DISENO.md`). **D3 CORRIDO (9 combos EURUSD/GBPUSD/XAUUSD × M1/M5/M15, null-permutación n=500, Bonferroni):** rechaza H0 (random walk) de forma CONSISTENTE en M1/M5/M15 → **criterio de INVARIANZA DE ESCALA CUMPLIDO**. **PERO SALVEDAD HONESTA (no confirmar doctrina):** (1) tamaño de efecto NEGLIGIBLE — EURUSD mAbsTurn 0.0007 vs null 0.0008 (≈0 en la práctica); XAUUSD 1.10 vs 1.17 (≈3%); (2) p<0.0001 está inflada por n≈60k barras (trampa de muestra gigante: cualquier desvío es "significativo"); (3) `cos_mean` era ENGAÑOSO en el reporte D3 original (1.0000 en todos los símbolos, inconsistente con mAbsTurn de XAUUSD 1.1): el bug era que `run_experiment.py` medía el coseno sobre el path original (escala anisótropa eje-tiempo vs precio), donde los vectores apuntan casi puros en +t → cos≈1.0 SIEMPRE (artefacto de aspect-ratio). Corregido (2026-07-23, agente + auditoría humana): `run_combo` reconstruye con `unit_path` (dt=1, alineado a `mean_abs_turn`), y ahora `cos_mean` es COHERENTE — XAUUSD 0.30 (giros reales), EURUSD/GBPUSD ~1.0 (casi rectos, mAbsTurn 0.0007, coherente). `core.py` NUNCA tuvo el bug (fórmula `angle_cosine` correcta). Reporte regenerado: `results/geometry_lab_d3_report.txt` + `.json`. | 🟢 D1+D2+D3 | Alta |
| **RE-BASELINE sequence post-v2.8 (EURUSD H4→M15)** | **Re-medir el camino sequence con datos frescos (costos ON, fixed2r, disp)** | 🟢 **CORRIDO (2026-07-23, exit 0):** `run_backtest.py --symbol EURUSD --htf H4 --ltf M15 --model intradia --counter-trend --tp-mode fixed2r` ⇒ 278 señales / 258 trades / 52.3% WR / **PF 1.155** / +20.0 R / salidas {SL:181, hold_limit:44, TP:33}. Resultado IDÉNTICO (byte-a-byte) para `--model po3` ⇒ **bandera de auditoría: PO3 parece flag muerto/alias en el dispatcher** (ver fila auditoría PO3 abajo). Mejora R6.4 previo (EURUSD -4.89) por costs ON + fixed2r + sequence. PF 1.155 pasa gate PF>1. Detalle en `results/baseline_turtle_ct.txt` + `results/baseline_po3.txt`. | 🟢 Cerrado (re-medición) | Alta |
| **Auditoría PO3 (flag muerto → fork a IMPLEMENTADO)** | **Diagnosticar y cablear `--model po3` para que no sea engañoso** | ✅ **IMPLEMENTADO (2026-07-23, fork a, commit pendiente de push):** `args.model` AHORA llega al motor. (1) `run_backtest.py:566` pasa `args.model`; (2) `run_sequence_backtest` y `run()` aceptan y propagan `model`; (3) `canonical.evaluate_signals(model=...)` aplica `filter_signals_by_model` → `model="po3"` FILTRA a señales con `po3_complete is True` (usa `compute_po3_complete` real de `po3_motor.py`, ya anotado en `canonical.py:483-509`); `model="scalping"` resuelve `exec_tf=M5`; `model="intradia"` sin filtro (regresión cero). El `tag` del reporte ahora incluye el modelo (`SEQ-CT-fixed2r-po3`). NOTA: la lógica PO3 real vive en `signals/po3.py` (`evaluate_po3`), pero en el backtest sequence el filtro por `po3_complete` es lo que hace que el flag signifique lo que dice (no alias). **EVIDENCIA:** 4 tests unitarios de `filter_signals_by_model` verdes (0.57s) + 8 tests de regresión del runner verdes (6.86s, `evaluate_signals(intradia)` idéntico a baseline) + trazabilidad estática runner→canonical línea a línea. Falta confirmación dinámica sobre datos reales (integration test `@pytest.mark.slow`, motor legacy lento en este hardware). Ver `tests/test_model_po3_wiring.py`. | ✅ Cerrada (fork a implementado) | Alta |

**Criterio de completitud:** A1-A11 + R0-R4 + libro 18 en 🟢. Harness 100%. A12 validado con datos suficientes. Solo entonces production-ready para bot.

---

## 4. Fases Futuras

- **Fase R4-clean:** ✅ CERRADA 2026-07-17. Turtle/sequence 6m + costos + sim fondeo (8%/DLL4%/MLL8%): **REJECT_NO_EDGE**. Informe: `docs/auditorias/R4_CIERRE_FUNDING_2026-07-17.md`. No auto-trade ICT.
- **Fase R3.5-libros:** escribir 21 (SMT), 22 (Breaker/MMXM), 23 (OTE) y cablear detectores (bloquea R4-honesto v30).
- **Fase R6 WF/OOS:** re-run A12 (**R5 datos ya cerrado**; no esperar descarga).
- **Fase R7 unificación:** un solo motor de evaluación ICT canónico (`sequence.py`) para backtest, UI y agente. El contrato R7 (R7_UNIFICACION_MOTOR.md) es la autoridad: `engine.py` queda degradado a helpers puros; `ict_agent` delega en `sequence`; `legacy/backtest` y `ml/dataset_builder` quedan DOCUMENTADOS FUERA de alcance de R7 (deuda a resolver post-R7, con decisión explícita). No se promete "matar todas las islas" en R7: el DoD de R7 exige que no haya motor ICT paralelo INVISIBLE, no la migración del legacy/ML en esta fase.
- **Fase Live (A8):** ÚLTIMA, solo con OK humano.
- **Fase R10/R11 — PRINCIPIOS ARQUITECTÓNICOS (motor de interpretación del mercado):** NUEVA dirección de nivel superior a R7, establecida 2026-07-15 (docs/plan/PRINCIPIOS_ARQUITECTONICOS.md). 4 reglas: (1) decisión SIEMPRE del estado/interpretación del mercado, NUNCA constante arbitraria; (2) modelamos mercado no velas (motor sobre MarketObjects+relaciones+contexto; IA sobre entidades); (3) 4 preguntas obligatorias antes de cualquier parámetro arbitrario; (4) interpretación contextual sobre regla fija, SI es objetiva/medible/reproducible/verificable. **NO se aplica dentro de R7 (congelado).** **PRIMER CANDIDATO R10 (registro 2026-07-15):** `bos_gap` (sequence.py=40 vs run_backtest.py=10) es número mágico antipatrón → derivar ventana de confirmación BOS de estado estructural del MarketObject, NO unificar el literal. T3.2B completado como borrado mecánico de isla sin tocar bos_gap; la divergencia 2-vs-5 queda como deuda R10. **R10 PROPUESTA A COMPLETADA Y COMMITEADA (2026-07-16, commit 057a44d, TDD):** `confirmation_window()` SIN INDICADORES (matemática pura rango high-low + tabla empírica de probabilidad de mitigación del backtest); `SequenceConfig.bos_gap: int|None` (40 fijo default / None dinámico); cableado en run_sequence vía `_effective_bos_gap`; tests en `tests/test_r10_bos_gap_dynamic.py`. **AUDITORÍA R10 vs PRINCIPIOS (2026-07-16):** cumple 100% solo P3 (proceso 4 preguntas); P1/P2/P4 parcial — en el fondo `confirmation_window` sigue siendo ventana en NÚMERO DE VELAS (tipo `int`, usos `índice - índice > N`). Es paso intermedio data-driven SIN indicadores, NO la arquitectura final. **R10.B EN PAUSA (2026-07-16, por decisión expresa):** calibrar tabla empírica real sobre histórico queda congelado; la fuerza `r` se relega a calidad/narrativa en R10.C/R11, no a caducidad. **R10.C + R11 — DISEÑO BORRADOR APROBADO (2026-07-16, commit 9ece465), implementación en curso por TDD:** docs/plan/DISENO_R10C_R11.md convierte los Principios en plan ejecutable. **R10.C FASE A (StateMachine, commit 0b3a332): COMPLETADA** — `ict_backtest/state_machine.py`, transición por evento, anti-timer. **R10.C FASE B (Invalidators, commit 0b3a332): COMPLETADA** — `ict_backtest/invalidators.py`: `rompio_swing_que_defendia`, `liquidez_tomada_sin_continuacion` (por precio), `bos_opuesto_en_misma_narrativa` (por grafo). **R10.C FASE C (ObjectGraph, commit 2026-07-16): COMPLETADA** — `ict_backtest/object_graph.py`: `add`/`link`/`parents`/`children`/`opuesto_en` por punteros (id), sin tiempo; B2 validado contra grafo real. **R10.C FASE D (MarketNarrative, commit 2026-07-16, TDD): COMPLETADA** — `ict_backtest/market_narrative.py`: cadena causal desde SWEEP raíz, `is_noise` para sueltos (sin grafo), `signal_objects` por estado. **R10.C FASE E (EventEngine + run_semantic + equivalencia, commit 2026-07-16, TDD): COMPLETADA** — `ict_backtest/event_engine.py`: `emit` por eventos (sin reloj), enlace causal BOS←SWEEP más cercano (zona cruzada, consumo único), `run_semantic` motor canónico; `translation.py` añade SWEEP persistente + zonas a BOS/CHOCH/FVG/OB. RED 2 REDEFINIDO: `Legacy ⊆ Semantic` + integridad causal (doc INFORME_EQUIVALENCIA_R10C.md). **R10.C FASE F (SemanticRules + IA sobre entidades, commit 2026-07-16, TDD): COMPLETADA** — `ict_backtest/semantic_scorer.py`: `SemanticScorer.score(objects, root)` deriva calidad/confianza de ESTADO+NARRATIVA+RELACIONES (entidades), rechaza DataFrame OHLC (TypeError); favorece narrativa completa (sweep->bos) sobre ruido. **R10.C COMPLETO (Fases A-F, TDD, 20 tests verdes).** R10.B EN PAUSA (ver arriba). Siguiente: R6 backtest honesto -> R5 datos -> A12 walk-forward -> A8 vivo.

---

## 5. Métricas de Éxito / Gate

- Profit Factor ≥ 1.25 (backtest 1.61 ✅ — pero ese número es del stack SMC heredado, NO del ICT puro R4).
- **R4 ICT puro:** gate PF ≥ 1.10 **y** meta fondeo 6m. **CERRADO:** SB/PO3/Turtle-sequence **sin edge** para live automatizado (2026-07-17).
- Win Rate ≥ 52% · Max DD ≤ 10% · Sharpe > 1 · Expectancy > 0 (del stack SMC, no R4).
- Edge diagnosis OOS PF ≥ 1.10 en >1 símbolo: ✅ (XAUUSD 1.376, etc.) — falta walk-forward A12.
- Trade count ≥ 200/backtest (actual 91 ⚠️).
- Harness: 100% escenarios antes de merge.

---

## 6. Próximos Pasos Inmediatos

1. ~~**R4-clean**~~ ✅ cerrado 2026-07-17 — sin edge ICT mecánico para fondeo/live.
2. **Producto observador:** app + vigilante + LIMIT demo (sin pretender edge). **AVANCE 2026-07-23 (SDD Cierre del Setup, `docs/plan/SDD_CIERRE_SETUP.md`):** tickets **5C** (run_cycle resiliente: canonical acotado + cache atómico, 9 tests), **5B** (POI libro 21: tier/anchored/stacked/quality_bonus como BONUS ≤20, 9 tests) y **5A** (trigger real: máquina de estados + gate de killzone, 16 tests) **IMPLEMENTADOS** con 34 tests GREEN y evidencia viva EURUSD real vía parquet en `docs/evidence/fase4_last_cycle_probe.json`. Fase C del plan del dashboard cerrada. **Fase 5 (deuda UI):** campos nuevos ya VISIBLES en widgets (`format_helpers.py` + market_state_widget/plan_strip_widget/main_window), 12 tests GREEN; los 3 estados honestos del canonical ahora se pintan. **Total SDD: 46 tests nuevos GREEN**. **CERRADO 2026-07-23: ~~ticket 5D (latencia M5/SMT, two-pass)~~ ✅** — `run_cycle` refactorizado two-pass: cache CORE (D1/H4/H1/M15) inmediato tras el pass 1 + enriquecimiento M5/SMT/canonical que re-escribe después (8 tests GREEN, `tests/test_run_cycle_twopass.py`). Pendiente solo: calibración de CANONICAL_TIMEOUT_S en máquina de producción.
3. ~~**R5 (datos multi-año M15)**~~ ✅ **CERRADO 2026-07-24 en disco** — `XAUUSD_M15`/`EURUSD_M15` ≥4.5 años. Inventario: `docs/DATA_STATUS.md`. (A6 residual: M5/M1 de los 5 majors menores — no bloquea A12 oro/euro.)
4. **A12 (PENDIENTE RE-RUN, desbloqueado por data):** walk-forward OOS celda `no_session`×XAUUSD con motor limpio post-R4/R6. El 1er pase falló con setup viejo. Deudas opcionales en paralelo: versionar `ict_backtest/v2/` (auth Ruben). **NO** volver a "bajar M15 del oro" como prerrequisito.
5. **R7:** completar deuda pipeline/ML/legacy o dejar documentada.
6. **R3.5:** ✅ CERRADO (Breaker+OTE+SMT todos cerrados con detectors, tests y wiring).
7. Cualquier nuevo desarrollo pasa por harness actualizado.
8. **Laboratorio de Geometría (D3, CERRADO 2026-07-23):** experimento real sobre EURUSD/GBPUSD/XAUUSD en M1/M5/M15 (9 combos) con null-permutación. **Veredicto: H1_CONFIRMADA con INVARIANZA DE ESCALA** (rechaza random walk en M1/M5/M15 y ≥2 símbolos) — PERO tamaño de efecto ≈ ruido (EURUSD mAbsTurn 0.0007 vs null 0.0008). `cos_mean` corregido (era artefacto de aspect-ratio en `run_experiment.py`; ahora XAUUSD 0.30, EURUSD/GBPUSD ~1.0 coherentes con mAbsTurn). 11 tests verdes. Reporte `results/geometry_lab_d3_report.txt` + `.json`.
9. **Auditoría PO3 (CERRADA 2026-07-23, fork a IMPLEMENTADO):** `--model` AHORA llega al motor. `model="po3"` filtra a señales con `po3_complete is True` (no alias); `model="scalping"` → exec_tf=M5; `model="intradia"` sin filtro (regresión cero). 4 tests unitarios + 8 de regresión verdes. **BUG encontrado y corregido (2026-07-24):** `canonical.evaluate_signals` leía el sesgo H4 por `iloc[s["entry_at"]]` (índice M15) en vez de por tiempo → leía vela H4 de 2021 para entrada M15 de 2026 (commit 011806e, reusa `closed_row_at_time`). **VEREDICTO test slow (2026-07-24, 59min):** `intradia=1` señal base en ventana 4000 M15 → el filtro no es alias (pasa) PERO la ventana es inservible para medir conteo PO3 (motor base produce 1 señal ahí). El fix corrige la lectura de contexto; su impacto en detección requiere correr sobre datos completos. Referencia cualitativa de prevalencia: `docs/evidence/po3_prevalencia_externa_IA.md` (análisis IA externa, ~460 PO3/año EURUSD, confirma que el 0 del motor era bug, no muestra escasa).
10. **Auditoría R6 formal (CERRADA 2026-07-23):** `docs/auditorias/AUDIT_R6_FORMAL_2026-07-23.md`. Sello v1 completo (G1+G2+G3, 15/15 tests, 3 bugs fixes). **DEUDAS (b)(c) CERRADAS 2026-07-24:** edge_diagnosis cap + DSR/PBO en grilla 168. **DEUDA (d) R5 datos XAUUSD M15: CERRADA 2026-07-24** (parquet multi-año verificado). Veredicto de edge pendiente en: (a) versionar `ict_backtest/v2/` (auth Ruben) y sobre todo **A12 WF multi-fold**. Re-baseline EURUSD PF 1.155 (costs ON) es primera señal positiva pero insuficiente sin WF.
11. **Dashboard operacional — deuda de veredicto (CERRADA 2026-07-24):** causa del cartel "No hay números de orden" resuelta. (a) Falso consenso eliminado en `app_observador/core/engine.py` (`_canonical_plan` ya NO fuerza `votes[LONG]=2`); (b) Régimen de mercado derivado de `avg_candle_range` (rango puro, sin ATR) cableado en `run_cycle` two-pass; (c) SMT Divergence cableado en el dashboard como bonus (`ict_backtest/setups/smt_divergence.py`, Opción B: anota, no filtra duro). Strength Score y ranking multi-activo quedan EN CONSTRUCCIÓN (campo ausente → nunca forzar, regla Ruben). Tests: 24 passed (regime_and_votes 5 + smt_divergence + orchestrator_flags + twopass). El semáforo sigue AMARILLO a propósito si no hay setup con R:R ≥ 1:2.
12. **Limpieza datos + límite intraday (2026-07-25):** eliminados stubs falsos `GBPUSD_M1.parquet` (50k velas 2026, no real) y `EURUSD_M30.parquet` (1k velas, basura). Probe Dukascopy confirmó: FX intraday solo desde **2012** (sin fuente con 20 años M1/M5/M15 para FX); XAUUSD sí tiene intraday desde **2006**. Los 20 años históricos reales son solo D1/H4/H1 (8 pares, 2006→2026). Estado post-limpieza: 6 TF completos = EURUSD, XAUUSD; GBPUSD 5 TF (sin M1); otros 5 majors 4 TF. Detalle en `docs/DATA_STATUS.md` (sección 2026-07-25).
13. **Diario ICT — rediseño pestaña Noticias (2026-07-27):** la pestaña `Noticias` dejó de ser un `QListWidget` RSS plano y ahora es el **"Diario ICT"**: informe en 3 columnas tipo periódico (masthead + dateline + section rules + bylines) generado desde datos REALES del motor (`engine.run_cycle`: estructura multitemporal, bias, veredicto, noticias rojas, semáforo). Usa design language de periódico (skills claude-design/baoyu-infographic/popular-web-designs traducidas al `QTextBrowser` PySide6). Acrónimos ICT/Engram (BSL/SSL/BOS/CHOCH/FVG/OB/OTE/POI/PO3/KZ/HTF/ITF/LTF/MSS/SMT/RR) marcados con tooltip + glosario al pie. Generación con LLM opcional en hilo aparte (no congela UI), cache por (día, franja horaria). Cableo: `main_window._update_principal` llama `noticias.set_result(result)`. Tests: `tests/test_news_ai.py` (8) + run_cycle (16) = 24 GREEN. Verificación visual: bylines/dateline/section rules/acrónimos confirmados en `QTextBrowser`.
14. **Semáforo pestaña Auto = VISOR FIEL del motor (2026-07-28):** el semáforo dejó de recalcular MT5/stochastic_signal/trend_context por su cuenta (cerebro redundante y DIVERGENTE de la pestaña Señal) y ahora es un visor del motor grande: lee `engine.load_cached()` (`data/blackbox/last_cycle.json`) y mapea las 4 luces en `_lights_from_cache(result)` — `extreme`/`cross` <- `stoch_m15` (el motor lo expone ahora en el cache, tras pass1), `trend` <- `context_alignment.alignment=='ALIGNED'`, `confirm` <- `stoch_m15.confirm AND trigger=='READY'`. Bajo cada luz hay una ista de checks (✓ verde / ✗ rojo) vía `_phase_details`: fase 1 y 2 = 1 check ciego (el motor solo tira el bool); fase 3 = 2 checks (cruce confirmado + motor en READY); fase 4 = 2 checks (macro alineado + intraday alineado). Así se ve POR QUÉ cada fase está en verde/rojo. Borrado sin código muerto: `_eval_phases`/`_context_direction`/`_read_m15`/`_ensure_mt5` + campos `_mt5`/`_ctx_cache`. `signals/stochastic_signal.py` NO se tocó (lo usa el runner del bot, sigue vivo). Single source of truth = motor. Tests: `tests/test_autopilot_from_engine.py` (8) + `tests/test_autopilot_semaphore.py` rewrite a nueva API (6) + `tests/test_autopilot.py` (4) = 18 GREEN.

---

*ÚNICA fuente de verdad a partir de 2026-07-14 (v2.3). Reemplaza versiones previas. Alineado con COMPLETION_REPORT.md, docs/auditorias/AUDIT_R4_FINAL_2026-07-13.md, docs/ict/18_EJECUCION_OPTIMA_TF_SL_ENTRY.md, y graph.json @ 46b074e (auditoría de islas ICT: 2 aristas cruzadas / 5 módulos).*
